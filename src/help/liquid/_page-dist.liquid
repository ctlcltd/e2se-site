{% if distributable %}      <div id="foot" role="contentinfo">
        <div>{% unless templateName == "index" %}{% assign helpPath = templateName | append: ".html" %}{% endunless %}{% assign helpUrl = helpBaseUrl | append: helpPath %}
          <p>Help revision (d/m/y): {{ help_rev | date: "%d/%m/%Y" }}<br>Online Help page link: <a href="{{ helpUrl | escape }}">{{ helpUrl }}</a></p>
          <p translate="no">Copyright &copy; <span>e2 SAT Editor Team</span> <span class="whoiam">[Leonardo Laureti]</span></p>
        </div>
      </div>{% else %}
      <!-- Help revision (d/m/y): {{ help_rev | date: "%d/%m/%Y" }} -->
{% endif %}