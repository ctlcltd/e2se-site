{% if distributable %}      <div id="foot">
        <div>
          <p>Help revision (d/m/y): {{ help_rev | date: "%d/%m/%Y" }} | <a href="{{ helpBaseUrl | escape }}">{{ helpBaseUrl }}</a></p>
          <p>Copyright &copy; e2 SAT Editor Team <span>[Leonardo Laureti]</span></p>
        </div>
      </div>{% else %}      <!-- Help revision (d/m/y): {{ help_rev | date: "%d/%m/%Y" }} -->
{% endif %}