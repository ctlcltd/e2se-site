{
  "name": "e2se-site",
  "version": "1.0.0",
  "description": "Static files for e2se-site",
  "scripts": {
    "watch:site": "npx grunt watch:site",
    "watch:help": "npx grunt watch:help",
    "watch:translate": "npx grunt watch:translate",
    "build:site": "npx grunt build:site",
    "build:help": "npx grunt build:help",
    "build:translate": "npx grunt build:translate",
    "build:bunch": "npx grunt build:bunch",
    "build:all": "npx grunt",
    "dist:help": "npx grunt dist:help",
    "copy:site": "npx grunt copy:site",
    "copy:translate": "npx grunt copy:translate",
    "copy": "npx grunt copy"
  },
  "author": "Leonardo Laureti",
  "license": "MIT",
  "devDependencies": {
    "clean-css": "^5.3.3",
    "grunt": "^1.6.1",
    "grunt-contrib-clean": "^2.0.1",
    "grunt-contrib-concat": "^2.1.0",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-cssmin": "^5.0.0",
    "grunt-contrib-sass": "^2.0.0",
    "grunt-contrib-watch": "^1.1.0",
    "grunt-terser": "^2.0.0",
    "liquidjs": "^10.18.0",
    "sass": "^1.80.2",
    "terser": "^5.36.0"
  }
}
